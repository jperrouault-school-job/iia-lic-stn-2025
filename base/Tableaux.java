// Tableaux
// Stocker plusieurs "variables dans une seule"
int[] monTab = new int[5];
monTab[0] = 1;
monTab[1] = 15;
// ...

int[] monTab2 = { 1, 15, 30, 5, 4 };

// Parcourir le tableau
for (int i = 0; i < monTab2.length; i = i + 1) {
    monTab2[i];
}
